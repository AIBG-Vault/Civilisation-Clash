So I came to this later and have no idea what has last been implemented. I did have some ideas:

# OVERHAUL OF THE ADMIN SYSTEM
There should be no "admin" users. Just have a server option "ClientCanOverrideOptions" or something like that. Basically, if set to true, the client can change any of the game options (do any of the previously privilleged instructions). If set to false, the
privilleged instructions wont work. Simple, and requires no admin system.

There should then be no traces of the admin functionality in the backend and both the frontend.